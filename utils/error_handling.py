# utils/error_handling.py

class ErrorHandler:
    def log_error(self, error_message, additional_info=None):
        """
        Log the error message along with any additional information.

        Args:
            error_message (str): The error message to be logged.
            additional_info (dict, optional): Additional information related to the error. Defaults to None.
        """
        # Implementation of logging logic goes here

    def handle_error(self, error_type, error_message):
        """
        Handle the error scenario and provide appropriate error handling logic.

        Args:
            error_type (str): The type of error.
            error_message (str): The error message.

        Returns:
            str: The formatted error message.
        """
        # Implementation of error handling logic goes here

    def format_error_message(self, error_type, error_message, additional_info=None):
        """
        Format the error message with relevant information.

        Args:
            error_type (str): The type of error.
            error_message (str): The error message.
            additional_info (dict, optional): Additional information related to the error. Defaults to None.

        Returns:
            str: The formatted error message.
        """
        # Implementation of error message formatting goes here
